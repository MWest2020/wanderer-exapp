# Screenshots

Drop store screenshots here (e.g. `dashboard.png`) and reference them
from `appinfo/info.xml`'s `<screenshot>` element. They are bundled into
the App Store release archive by `scripts/appstore-release.sh`.
